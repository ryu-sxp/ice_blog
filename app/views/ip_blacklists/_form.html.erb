<%= form_with(model: ip_blacklist, local: true) do |form| %>
  <% if ip_blacklist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ip_blacklist.errors.count, "error") %> prohibited this ip_blacklist from being saved:</h2>

      <ul>
      <% ip_blacklist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ip %>
    <%= form.text_field :ip, id: :ip_blacklist_ip %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration, id: :ip_blacklist_duration %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
